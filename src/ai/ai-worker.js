/**
 * Web Worker for async AI computation.
 * This runs the AI in a separate thread to keep the UI responsive.
 *
 * Note: Web Workers with ES modules require specific server configuration.
 * This is a simplified version that uses importScripts-compatible approach.
 * For the initial implementation, AI runs on the main thread with setTimeout.
 */

// Placeholder - AI currently runs on main thread via setTimeout
// to avoid complexity with module workers.
// Can be upgraded to a proper Web Worker when needed.
